{
  "name": "workday-synthesizer",
  "description": "CLI tool that synthesizes your workday from scattered developer signals into a focused prioritized plan",
  "version": "0.2.0",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/emin93/workday-synthesizer"
  },
  "bin": {
    "workday": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "npm run build && node dist/cli.js",
    "lint": "eslint src/ --ext .ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "handlebars": "^4.7.8"
  }
}
